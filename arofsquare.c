// Write a program to calculate area of sqaure  (side given)

#include<stdio.h>
int main() {
    int side;
    printf("enter side");
    scanf("%d", &side);

        printf("area is : %d", side * side);
        return 0;
}